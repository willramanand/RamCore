name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Build and test
        run: mvn -B verify

      - name: Upload plugin jar
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: RamCore-jar
          path: target/RamCore-*.jar
          if-no-files-found: error
